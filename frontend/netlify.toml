# netlify.toml

[build]
  # Base directory where package.json exists
  base = "frontend"
  # Vite build command
  command = "npm run build"
  # Publish directory relative to base
  publish = "dist"

# React Router SPA redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
